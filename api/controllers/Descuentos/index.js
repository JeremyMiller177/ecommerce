const { getDescuentos } = require('./getDescuentos')
const { getDescuento } = require('./getDescuento')
const { createDescuento } = require('./createDescuento')
const { updateDescuento } = require('./updateDescuento')

module.exports = {
  getDescuentos,
  getDescuento,
  createDescuento,
  updateDescuento
}
